import LatLon, { Cartesian, Vector3d, Dms } from 'https://cdn.jsdelivr.net/npm/geodesy@2/latlon-ellipsoidal.js';
/*$(document).ready(function(){
    $("#map").on("mousemove", function(){
        let cursorLatLon = new LatLon($("#cursorLat").children("p").children("span").html(), $("#cursorLong").children("p").children("span").html(),);
    });
})*/
